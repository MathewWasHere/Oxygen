# OXYGEN — Rebrand Notes

## مبنا

این پروژه یک کپی تمیز از پروژه مبدأ است و تاریخچه گیت مبدأ را ندارد. تمام نام‌ها، متن‌ها، متادیتا، نام پکیج و دارایی‌های بصری برای فست فود اکسیژن بازنویسی شده‌اند.

## تغییرات اصلی

1. `src/lib/data/catalog.ts`
   - اطلاعات مجموعه اکسیژن، تلفن، آدرس، اینستاگرام و لینک نقشه ثبت شد.
   - مختصات دقیق گوگل‌مپ: `28.9505657, 53.6252939`.
   - ۹ دسته و ۴۱ آیتم از روی نام فایل‌های عکس ساخته شد.
   - برای هر ۴۱ آیتم قیمت فرضی اولیه ثبت شده است.

2. `public/food/`
   - همه عکس‌های قدیمی حذف شد.
   - برای هر یک از ۴۱ عکس اکسیژن، دو خروجی WebP در ۸۰۰ و ۴۰۰ پیکسل ساخته شد.

3. `public/brand/`
   - لوگوهای رسمی از `OxygenLogoPack.html` استخراج شد.
   - واریانت‌های تیره، سفید، قرمز، کریمسون و طلایی به PNG/WebP ساخته شد.
   - JSON دارایی‌ها و راهنمای کوتاه برند اضافه شد.

4. PWA و SEO
   - favicon، آیکون‌های ۱۹۲/۵۱۲، maskable و Apple Touch بازتولید شد.
   - manifest، Open Graph، JSON-LD و عنوان‌های صفحات برای اکسیژن اصلاح شد.

5. Hero
   - تصاویر محیطی مجموعه مبدأ حذف شد.
   - هدر جدید از سه عکس واقعی منوی اکسیژن ساخته شد.

6. فونت
   - سه فایل ارسالی SnappWeb 2.0 با وجود پسوند `.txt`، فایل WOFF معتبر بودند.
   - فایل‌ها بدون دستکاری باینری با پسوند درست `.woff` در `public/fonts/` قرار گرفتند.

7. اطلاعات فرضی تکمیلی
   - قیمت تمام آیتم‌ها به‌صورت فرضی ثبت شد.
   - ساعت کاری فرضی: همه‌روزه ۱۱:۰۰ تا ۰۱:۰۰.
   - شعار فرضی: «هر لقمه، یک نفس تازه».
   - هزینه و محدوده‌های ارسال مطابق نسخه قبل باقی ماند.

## بررسی نهایی

- `npm install`: موفق، بدون آسیب‌پذیری شناخته‌شده
- `npm run lint`: موفق
- `npm run build`: موفق
- ۶۳ صفحه استاتیک/SSG تولید شد.
- مرجع شکسته به عکس‌های قدیمی: صفر
- عبارت‌ها یا فایل‌های باقی‌مانده از برند مبدأ: صفر

## نیازمند تأیید مجموعه

- دامنه نهایی وب‌سایت
- هزینه و محدوده‌های دقیق ارسال
